{
  "name": "functions",
  "description": "Cloud Functions for Firebase Absensi SMK NU",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^11.5.0",
    "firebase-functions": "^4.2.1",
    "express": "^4.18.2",
    "moment-timezone": "^0.5.43",
    "bcryptjs": "^2.4.3",
    
    // --- Library untuk Fitur Tambahan ---
    // LIBRARY EKSPOR EXCEL (Contoh, jika Anda menggunakan function exportExcel)
    "exceljs": "^4.3.0", 
    
    // Catatan: Library WA Bot harus ditambahkan di sini jika menggunakan solusi Node.js API.
    // Contoh: 'whatsapp-web.js': 'versi_terbaru' (jika Anda menggunakan library self-hosted)
    "cors": "^2.8.5" // Berguna untuk mengizinkan request dari frontend
  },
  "devDependencies": {
    "firebase-functions-test": "^3.0.0"
  },
  "private": true
}
